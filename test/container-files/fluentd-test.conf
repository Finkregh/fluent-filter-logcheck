<source>
  @type sample
  sample {"message":"world"}
  tag sample
  <parse>
    @type syslog
  </parse>
</source>

# <source>
#   @type tail
#   path /var/log/httpd-access.log
#   pos_file /var/log/td-agent/httpd-access.log.pos
#   tag apache.access
#   <parse>
#     @type syslog
#   </parse>
# </source>

<filter example-logcheck.ignore>
  @type logcheck
  rule_type ignore
  # sshd-session\[[[:digit:]]+\]: fatal: recv_rexec_state: ssh_msg_recv failed
  rules_file /etc/fluent/logcheck/ignore.logcheck
</filter>

<match *.**>
  @type copy

  <store>
    @type stdout
  </store>
</match>
